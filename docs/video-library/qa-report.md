# LO Development Video Library Sprint — QA Report

Author: Power Agent 8 (QA & Release Runbook)
Date: 2026-05-31
Scope: Read-only QA across the full sprint output. The Lead runs the authoritative
`lint` / `typecheck` / `build`; this report is a static + manifest cross-check layer.

Verdict legend: **PASS** / **FAIL** / **WARN** (non-blocking, flag for review) /
**N/A (Lead-owned)**.

> Methodology: every numeric finding below was produced by parsing the actual files on disk
> (data files JSON-parsed, manifests, draft JSON/CSV, generator scripts) and
> cross-referencing `.video-source/manifests/`. The library array is `JSON.stringify`'d so
> its keys are quoted (`"id"`, `"hostingStatus"`, …); checks account for that.

---

## 0. Summary scoreboard

| # | Check | Result |
|---|-------|--------|
| 1 | PA2 type contract (`LoVideo`, exports) matches spec | **PASS** |
| 2 | PA7 type contract (`LoVideoContent`, exports) matches spec | **PASS** |
| 3 | Entry counts (91 clips + 7 long-form = 98) | **PASS** |
| 4 | ID scheme + exact 1:1 manifest cross-reference | **PASS** |
| 5 | Content layer keyed to library IDs (98/98 set match) | **PASS** |
| 6 | No fabricated YouTube IDs / URLs in PA2 + PA7 data | **PASS** |
| 7 | Google Drive URLs are real (sourced from manifest), not invented | **PASS** |
| 8 | `hostingStatus` honesty | **PASS** (see §8 note on long-form) |
| 9 | `uploadStatus` carries real manifest values (not invented) | **PASS** |
| 10 | No `/Users` or `/Volumes` absolute path leakage in data | **PASS** |
| 11 | Naming guard across all changed files (real violations vs guard text) | **PASS** |
| 12 | Modified existing pages clean (naming, URLs, default exports) | **PASS** |
| 13 | LO Development route exists + valid Next 16 async params | **PASS** |
| 14 | AI Advantage embeds: real 11-char IDs, no placeholders, embed/ID consistent | **PASS** |
| 15 | AI Advantage embeds: all `unlisted`, notify `NO`, safe | **PASS** |
| 16 | PA3 Drive uploader: dry-run default, `--confirm`-gated, fail-closed, unwired | **PASS** |
| 17 | PA3 YouTube draft + n8n workflow: unlisted, notify=false, `active:false` | **PASS** |
| 18 | Drive/YouTube/n8n: nothing uploaded or activated this sprint | **PASS** |
| 19 | Source folders unmodified (read-only honored) | **PASS** |
| 20 | No 7.7GB video bulk-copied into `public/` | **PASS** |
| 21 | Long-form: real Drive URL present but `hostingStatus` still `google_drive_ready` | **WARN — status understated, decide policy** |
| 22 | Long-form source: 8 `.md` vs 7 `.mp4` | **WARN — source-side, agents correctly used 7** |
| 23 | Drive runbook G2: connected Google account is personal, not Loan Factory | **WARN — real pre-upload blocker, caught by PA3** |
| L | `lint` / `typecheck` / `build` authoritative pass | **N/A (Lead-owned)** |

No **FAIL** items. Three **WARN** items, all flagged for Jeremy/Lead decision; none is a
fabrication and none blocks the shipped data layer or route from compiling/rendering safely.

---

## 1. PA2 type contract — PASS

`src/data/loDevelopmentVideoLibrary.ts` (106,917 bytes) exports, per spec:
`VideoHostingStatus` (7-member union), `VideoSourceType`, `LoVideo`,
`loDevelopmentVideoLibrary: LoVideo[]`, `getLoVideoBySlug(...)`, `loVideoLibraryStats`.
Documented + generated by `scripts/build-video-library.mjs` (per
`docs/video-library/data-model.md`); the array is `JSON.stringify`'d → valid TS by
construction.

## 2. PA7 type contract — PASS

`src/data/loDevelopmentVideoContent.ts` (85,335 bytes) exports `LoVideoContent`,
`loDevelopmentVideoContent: LoVideoContent[]`, `getLoVideoContent(...)`. All 10 content
fields present on 98/98 entries.

## 3. Entry counts — PASS

`sourceType: "clip"` → **91**; `"long_form"` → **7**; total distinct IDs → **98**.
0 duplicate IDs in either file.

## 4. ID scheme + manifest cross-reference — PASS

91 clip IDs (`lo-dev-001`…`091`) + 7 long-form (`lo-longform-01`…`07`). Clip IDs vs
`master_clip_manifest.csv` `id` column: **exact set match** (91 = 91, 0 diff). Long-form
IDs map to the 7 `source_files_matched` keys in `build_summary.json`.

## 5. Content layer keyed to library — PASS

Library ID set **==** Content ID set (verified set-equality): 0 missing, 0 extra,
0 duplicates either side.

## 6. No fabricated YouTube metadata — PASS

`youtubeVideoId` = `null` on 98/98; `youtubeEmbedUrl` = `null` on 98/98. No
`youtube.com`/`youtu.be` strings anywhere in the library. Nothing has been uploaded to
YouTube → correctly null.

## 7. Google Drive URLs are real, not invented — PASS

(JSON-parsed the library array.)
- **Clips (91):** `googleDriveUrl` = `null` on 91/91.
- **Long-form (7):** `googleDriveUrl` populated on 7/7 with real
  `https://drive.google.com/open?id=...` links.

These 7 links are **NOT fabricated**. The generator `scripts/build-video-library.mjs`
(line 247) calls `extractDriveLink(reportText)`, which pulls the Drive URL **verbatim from
each long-form Gemini `.md` report** (the `source-reports/*.md` files, also mirrored in
`docs/lo-development-long-form-training-map.md` and
`docs/lo-development-video-training-metadata.json`). Verified: the link
`drive.google.com/open?id=1o-RK-AJXzd4j8unCqR4AeJAKaPwppNY9` appears literally in the
"1003 Mistakes to Avoid" report. They are the original Drive locations of the long-form
recordings, extracted from the read-only source. Source-grounded, no invention.
(Note: `build_summary.json` `source_files_matched` does NOT carry these links — the report
text is the real source.)

## 8. hostingStatus honesty — PASS (with a long-form note)

`hostingStatus: "google_drive_ready"` → 98/98. No `youtube_live` / `youtube_failed` /
`youtube_pending` / `local_only` / `needs_review`. No entry claims a YouTube-live state.
See §21 for the long-form `google_drive_ready` vs `google_drive_live` nuance.

## 9. uploadStatus carries real values — PASS

`uploadStatus`: `"youtube_hold"` → 91 (real value from
`youtube_upload_manifest_ready.csv`), `"pending"` → 7 (long-form, not in the youtube clip
manifest). Documented in `data-model.md`. Not invented.

## 10. No absolute path leakage — PASS

`/Users/` → 0, `/Volumes/` → 0 in the library. Path fields are repo-relative basenames
(e.g. `ai-support__using-the-loan-factory-ai-assistant__1003-mistakes.mp4/.jpg/.srt/.md`).

## 11. Naming guard — PASS

Real violations (excluding guard-rule text): `Tuan` → 0, `Apex Advisor` → 0, paid `Elite`
→ 0, rickroll `dQw4w9WgXcQ` → 0. The mentions in
`docs/video-hosting-replacement-workflow.md` and `docs/video-library/youtube/metadata-
template.md` are **enforcement language** — e.g. "`Thuan` (never `Tuan`)", "never reference
… 'Apex Advisor', or a paid 'Elite' tier" — which is correct, not a violation.

## 12. Modified existing pages — PASS

`ai-training/page.tsx`, `ai-training/video-library/page.tsx`,
`ai-training/video-library/[slug]/page.tsx`, `training-library/page.tsx`,
`training-library/clips/page.tsx`: all CLEAN — no naming violations, no rickroll, no
hardcoded youtube/drive URLs, all retain a default export.

## 13. LO Development route — PASS

- `src/app/lo-development/page.tsx` → default export `LoDevelopmentPage` (role dashboard;
  does not import the AI Advantage embeds — no coupling risk).
- `src/app/lo-development/ai-advantage/[...slug]/page.tsx` → default **async** export,
  Next 16 async `params: Promise<{ slug: string[] }>`, `generateStaticParams()` over
  `aiAdvantageYoutubeEmbeds`, `notFound()` guard, `redirect()` to canonical
  `lessonViewerPath`. Imports `aiAdvantageYoutubeEmbeds` +
  `getAiAdvantageYoutubeEmbedByLessonPath` by exact export names.
- Components `AiAdvantageUploadedVideoExplorer.tsx` (client, default export) +
  `AiAdvantageUploadedVideoCard.tsx` (default export) import the data + type by exact
  names; use brand classes (`card`, `btn-primary`, `btn-secondary`, `lf-*`); include a UI
  safety note (unlisted / no public / no notify).

## 14. AI Advantage embeds — real IDs, no placeholders — PASS

`src/data/aiAdvantageYoutubeEmbeds.json` (63 video rows; channel "Jeremy McDonald - The
Mortgage Mentor @jeremymcdonaldmortgage"): `totals` = `total_manifest_rows: 63,
uploaded: 63, still_ready: 0, failed: 0, skipped: 0`.
- `youtube_video_id`: **63/63 real, all exactly 11 chars** (e.g. `c8zULkTrwGU`). **0 blank,
  0 rickroll.**
- embed-URL/ID consistency: **0 mismatches**.
- The `.ts` (`aiAdvantageYoutubeEmbeds.ts`) maps the JSON and gates any source-issue row out
  of the embeddable list; all 63 are clean → 63 embeddable, 0 source-issues.

These are EXISTING already-on-channel videos being embedded, not new uploads from this
sprint. (NOTE for context: an earlier interim QA pass in this session, run while tooling was
returning empty output, briefly read a different snapshot of this file showing 91 blank/READY
rows and floated a `dQw4w9WgXcQ` placeholder hypothesis. The definitive on-disk check
confirms 63 real IDs and **zero** placeholders.)

## 15. AI Advantage embeds — safety settings — PASS

`privacy: unlisted` → 63/63; `notify_subscribers: NO` → 63/63. `safety` block:
`public_videos_created: NO`, `subscriber_notifications_sent: NO`, `pii_rows_uploaded: NO`,
`duplicate_uploads_detected: NO`, `all_uploaded_unlisted: YES`.

## 16. PA3 Drive uploader — fail-closed — PASS

`scripts/drive-upload/upload-to-drive.mjs` (8,661 bytes): dry-run by default; real upload
requires **all** of `--confirm`, `--account`, `DRIVE_UPLOAD_ACCOUNT`,
`LO_DEV_DRIVE_UPLOAD_APPROVED="yes"`; on any unmet gate it refuses (exit 2); and even with
all gates green, `performUpload()` is **intentionally unwired and throws** (exit 1) so no
accidental upload can occur. `scripts/drive-upload/build-drive-manifest.mjs` generates
`local-to-drive-mapping.csv` (387 rows; `drive_file_id`/`drive_file_url` = 0 populated;
`planned_drive_folder` planned for 387). Runbook: `google-drive/upload-runbook.md`.

## 17. PA3 YouTube draft + n8n workflow — PASS

- `docs/video-library/youtube/upload-queue-manifest.json` (91 clips): defaults
  `privacy: unlisted`, `notifySubscribers: false`, `madeForKids: false`, batch 5/day;
  every row `pending`, `unlisted`, `notify=false`, `driveFileLink/youtubeVideoId/…` = null.
- `automation/n8n/youtube-upload-workflow.draft.json`: **`active: false`**, 9 nodes, privacy
  unlisted wired, OAuth credential left as `REPLACE_WITH_…` placeholder (cannot run as-is).
- `youtube/activation-checklist.md`: a thorough, all-unchecked approval gate with explicit
  hard-stops (no public, no subscriber notify, no over-batch, no source mutation, no
  fabricated URLs, no activation before Jeremy's approval).

## 18. Nothing uploaded or activated — PASS

Clips: 0 Drive links, 0 YouTube IDs. n8n: `active:false` + placeholder credential. The 7
long-form Drive links pre-exist in the source manifest (not uploaded this sprint). The 63 AI
Advantage embeds reference pre-existing channel videos. No upload, no n8n trigger occurred.

## 19. Source folders unmodified — PASS

- Cutdown Library: 91 `.mp4`, 91 `.jpg`, 91 `.srt`, 110 `.md`, 6 `.json`, 3 `.csv`.
  Modified in last 12h: **0**.
- Long-form folder (name has a trailing space): 7 `.mp4` + 8 `.md`. Modified in last 12h:
  **0**.

## 20. No bulk video copy into public/ — PASS

Video files (`.mp4/.mov/.webm`) in `public/`: **0**. The 7.7GB source set was NOT staged
into the repo. (Pre-existing >5MB files in `public/` are brand logos + audio companions,
unrelated to this sprint.)

## 21. Long-form: real URL but status `google_drive_ready` — WARN (policy decision)

All 7 long-form entries carry a real `googleDriveUrl` (from the source manifest) yet keep
`hostingStatus: "google_drive_ready"` rather than `"google_drive_live"`. This is **not a
fabrication** (the inverse risk the contract warns about). It is an **understated** status:
a real Drive URL does exist.

Decision for Jeremy/Lead: is `google_drive_ready` deliberate because these are the
*source-folder* Drive copies (not the platform's curated/published hosting)? If so, fine —
document it. If these source Drive links are intended to be the live hosting for long-form,
the 7 entries should be `google_drive_live`. Either way it is consistent across all 7 and
honest about the URL's existence. (Clips correctly stay `google_drive_ready` with null
URLs.)

## 22. Long-form 8-.md vs 7-.mp4 — WARN (source-side)

The long-form source folder holds **8** `.md` but **7** `.mp4`. The extra `.md` is
`LO Support Research Analysis.md` — an analysis doc, not a recording. Agents correctly
grounded to **7** long-form entries (= 7 `.mp4` = the 7 `source_files_matched`). No
fabricated 8th video. Confirm with the source owner; no agent action needed.

## 23. Drive account gate G2 fails — WARN (real pre-upload blocker, caught by PA3)

`google-drive/upload-runbook.md` Step 0 records that the connected Google account is
`jeremy@mcdonald-mtg.com` (personal) — **not** the Loan Factory account
(`jeremy.mcdonald@loanfactory.com`) — and marks gate **G2 = FAILS**, instructing a STOP
until the account is corrected or Jeremy explicitly accepts the personal account. This is
exactly the kind of safety catch we want before any 7.7GB upload. Surfaced here so it is on
Jeremy's radar before Step 2 of the runbook.

## L. lint / typecheck / build — N/A (Lead-owned)

Authoritative gates are the Lead's. Static review found no obvious blockers: PA2/PA7 data are
valid TS (JSON.stringify'd array); the AI Advantage `.ts`, components, and `[...slug]` route
are valid Next 16 TSX with exact-name imports and async `params`; the `.mjs` scripts are
codegen/uploader utilities outside the Next build graph.

---

## Cross-agent contract reminders (exact names downstream code imports)

From `@/data/loDevelopmentVideoLibrary`: `loDevelopmentVideoLibrary`, `getLoVideoBySlug`,
`loVideoLibraryStats`, types `LoVideo`, `VideoHostingStatus`, `VideoSourceType`.
From `@/data/loDevelopmentVideoContent`: `loDevelopmentVideoContent`, `getLoVideoContent`,
type `LoVideoContent`.
From `@/data/aiAdvantageYoutubeEmbeds`: `aiAdvantageYoutubeEmbeds`,
`aiAdvantageYoutubeEmbedRows`, `aiAdvantageYoutubeEmbedSourceIssues`,
`aiAdvantageYoutubeEmbedValidation`, `aiAdvantageYoutubeEmbedModuleGroups`,
`groupAiAdvantageYoutubeEmbedsByModule`, `getAiAdvantageYoutubeEmbedByLessonPath`,
`getAiAdvantageYoutubeEmbedByLessonSlug`, `getAiAdvantageYoutubeEmbedModuleGroups`,
`getAiAdvantageYoutubeEmbedStats`, type `AiAdvantageYoutubeEmbedRecord`.

All present and correct. Any consuming UI must import these exact symbols.
